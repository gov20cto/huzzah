development:
  host: localhost
  database: huzzah_development

test:
  host: localhost
  database: huzzah_test

# set these environment variables on your prod server
production:
  uri: <%= ENV['MONGOHQ_URL'] %>
